server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  application:
    name: myproject-app
  datasource:
    url: "jdbc:mysql://localhost:3306/db_weather_app"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6380
      timeout: 5000ms
trackasia:
  key: ae3e5bdef671763880184dcce3001e385d